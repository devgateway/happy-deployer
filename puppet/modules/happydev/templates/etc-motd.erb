
Welcome to the <%= @project_environment %> environment for <%= @project_hostname %>

NOTE, in SL6 psql has 'local socket' for default hostname, be sure to use:
  psql -U $USER -W -h localhost $DATABASE

For any suggestions: https://github.com/devgateway/happy-deployer/issues

